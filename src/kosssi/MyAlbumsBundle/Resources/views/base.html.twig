{% extends '::base.html.twig' %}

{% block body %}
    {% block header %}
        <header{% block header_attr %}{% endblock %}>
            {% block header_content %}
                <a href="{{ path('album') }}"><i class="fa fa-home"></i></a>
                <h1>{{ block('title') }}</h1>
                {% spaceless %}
                    <div class="actions">
                        <a href="">
                            <i class="fa fa-download"></i>
                        </a>
                        <a href="" class="show-upload" title="{{ "Add photos" | trans({}, "album") }}">
                            <span class="fa-stack fa-lg">
                                <i class="fa fa-picture-o fa-stack-2x"></i>
                                <i class="fa fa-plus fa-stack-1x inverse"></i>
                            </span>
                            <span class="fa-stack fa-lg" style="display: none">
                                <i class="fa fa-minus"></i>
                            </span>
                        </a>
                        <div class="upload">
                            <div>
                                <form action="{{ oneup_uploader_endpoint('album') }}" class="dropzone" id="my-dropzone">
                                    <input type="hidden" name="album_id" value="{{ entity.id }}">
                                    <span data-description="{{ "Drop files here to upload" | trans({}, "album") }}"></span>
                                </form>
                            </div>
                        </div>
                        <a href="" title="{{ "Create a photo album" | trans({}, "album") }}">
                            <span class="fa-stack fa-lg">
                                <i class="fa fa-book fa-stack-2x"></i>
                                <i class="fa fa-plus fa-stack-1x inverse"></i>
                            </span>
                        </a>
                        <span class="remove-album">
                            <a href="">
                                <i class="fa fa-times-circle-o"></i>
                            </a>
                            {{ form(delete_form) }}
                        </span>
                    </div>
                {% endspaceless %}
            {% endblock %}
        </header>
    {% endblock %}
    {% block section %}
        <section{% block section_attr %}{% endblock %}>
            {% block section_content %}
            {% endblock %}
        </section>
    {% endblock %}
    {% block footer %}
        <footer{% block footer_attr %}{% endblock %}>
            {% block footer_content %}
            {% endblock %}
        </footer>
    {% endblock %}
{% endblock %}
